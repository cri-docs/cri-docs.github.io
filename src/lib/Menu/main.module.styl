nav
  position absolute
  bottom 0
  left 0
  width var(--nav-width)
  padding var(--padding)
  transform translateY(100%)
  ul
    display flex
    flex-direction column
    margin 0
    padding 0
    gap .3lh
    max-width 450px
    list-style none
    .label
      display flex
      align-items flex-start
      gap 1ch