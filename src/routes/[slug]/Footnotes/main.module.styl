.container
  position fixed
  bottom 0
  right 0
  width calc(var(--nav-width) - var(--padding) * 2)
  max-height 80vh
  min-width 300px
  overflow-y scroll
  overflow-x hidden
  padding-inline var(--padding)
  background-color var(--color)
  color white
  font-size 0.8em
  z-index 10
